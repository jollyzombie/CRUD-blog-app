import { Container } from 'react-bootstrap';
import Posts from '../../features/Posts/Posts';

const Home = () => {
  return (
    <Container>
      <Posts />
    </Container>
  );
};

export default Home;
